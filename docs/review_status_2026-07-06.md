# Review Status Update — 2026-07-06

Re-verification of P1 findings from `docs/code_review_2026-07-01.md` (work item W0
of the orchestrated fix run). Line numbers below are current as of this sweep;
the original report's line numbers are ~5 days stale. Several files were being
edited by other concurrent agents at the time of this sweep (`well_sort.py`,
`jp_calibration.py`, `pf_scenario.py`) — see notes per row.

| ID | Status | Evidence (file:line) | Notes |
|----|--------|----------------------|-------|
| P1-1 | FIXED | `woffl/flow/jetflow.py:634-651` (`_throat_mixture_anchor`), used at `woffl/assembly/solopump.py:229-236` to build `qtm_std` fed to both `diffuser_discharge` and `production_top_down_press`, and inline at `jetflow.py:804-805` | Water-mode anchor now includes `qoil_std + qnz_bwpd` when `wc_tm >= 1.0`, exactly the fix the finding asked for. |
| P1-3 | FIXED | `woffl/flow/jetflow.py:560-605` (`_throat_discharge_bracketed`) | Scan direction reversed: now scans **downward from `hi`**, docstring at lines 569-578 explicitly documents "the original upward scan locked onto the low root... false 'pump can't lift'" and that this now returns the high/physical root. |
| P1-5 | FIXED | `woffl/flow/outflow.py:82-91` (`beggs_diff_press`) | `ek = tp.beggs_ek(pin, rho_mix, vmix, vsg)` now computed and both `dp_stat`/`dp_fric` divided by `(1 - ek)`. Comment explicitly references the canonical B&B acceleration term. Note: memory log records outflow_test as pre-existing red from this same patch — not fixed by this sweep, just observed. |
| P1-6 | FIXED | `woffl/geometry/wellprofile.py:246-253` (`_horz_dist`, clamps `vd_diff` to `±abs(md_diff)`, comment cites "P1-6"); `woffl/flow/twophase.py:224-231` (`beggs_flow_pattern` raises typed `FlowPatternUnknown` instead of returning `"unknown"`) | Both halves of the finding (silent NaN + bare KeyError) addressed. `FlowPatternUnknown` confirmed as a `JetPumpError` subclass in `woffl/flow/errors.py:29`. |
| P1-7 | FIXED | `woffl/flow/jetplot.py:255-265` (`_dete_zero`) | Mask is now trimmed to its leading contiguous `True` run before flipping for `np.interp`; comment cites "P1-7" and explains the non-contiguous-mask garbage-output bug it replaces. |
| P1-8 | OPEN | `woffl/assembly/solopump.py:322` (`_residual_walk_inward`: `except ConvergenceError:` only) and `woffl/assembly/solopump.py:729` (bisection midpoint: `except ThroatEntryNoSolution:` only) | `woffl/flow/jetflow.py:282-283` (`nozzle_velocity`) raises a bare `JetPumpError` (not `ConvergenceError`) when `pni <= pte`; neither the walk-inward loop nor the bisection midpoint catches that, so it still propagates uncaught out of the fallback path exactly as described. Both sub-issues from the finding remain unaddressed. |
| P1-9 | OPEN | `woffl/flow/singlephase.py:178-180` (`ffactor_darcy`) | `if reynolds < 4000: ff = 64 / reynolds` — still no `reynolds == 0` guard; bare `ZeroDivisionError` still possible on zero-flow inputs. |
| P1-10 | OPEN | `woffl/pvt/formgas.py:230-247` (`_zfactor_grad_school`) | Cubic still unguarded (`zfactor = 1 - m*ppr + n*ppr**2 + 0.0003*ppr**3`), no clamp; `_zfactor_dak` alternative remains commented out at line 105. |
| P1-11 | FIXED | `woffl/gui/tab_helpers.py` (`pump_at_test_matches`, wraps `get_pump_at_date`); used in `woffl/gui/tabs/batch_run.py:471-477` (comment "P1-11 guard") and `woffl/gui/tabs/power_fluid_range.py:343-364` (`_render_calibration_flags_for_installed_pump`) | Both call sites now block/pause calibration when the installed pump differs from the pump-at-test-date, via one shared helper (the R-7 pattern). `batch_run.py` also has a dedicated mismatch banner (`_render_batch_hero_pump_mismatch`, referenced near line 813). |
| P1-13 | OPEN | `woffl/gui/utils.py:1264` (`run_jetpump_solver`), `:1495` (`run_batch_pump`), `:1596` (`run_power_fluid_range_batch`) all still accept `rho_pf`; docstrings now say "used for display only" (`:1516`, `:1619`); never passed into `FormWater`/`prop_pf` construction | `woffl/gui/pad_optimize.py:194,273,532,701,801` still hardcode `rho_pf=62.4` for `PowerFluidConstraint`, unrelated to the I/M plant's real PF SG (~1.03-1.04). Docstring update suggests the team is aware it's cosmetic but hasn't wired it or removed it. |
| P1-14 | OPEN | `woffl/gui/tabs/batch_run.py:901-1055` (Nelder-Mead comparison section) | When the calibration toggle is ON, `batch_pump.df` is mutated in place with the calibration factor (~lines 1224-1229) before this section reads its "current" baseline row, while the NM search itself (`woffl/assembly/batchpump.py` `search_run`) is raw physics — the displayed delta (~line 1044) still compares a calibrated seed to a raw NM output. |
| P1-15 | OPEN | `woffl/gui/tabs/well_profile.py:61` (`max_deviation`) and `:146` (deviation series feeding the "Horizontal Deviation" plot) | Both still compute `md_ray[i] - vd_ray[i]`; no reference anywhere in the file to `WellProfile.hd_ray` (the Pythagorean value populated at `woffl/geometry/wellprofile.py:52`). |
| P1-16 | OPEN | `woffl/gui/utils.py:1640-1642` (`np.arange(power_fluid_min, power_fluid_max + power_fluid_step, power_fluid_step)`) | No clip to `power_fluid_max`. `woffl/gui/tabs/power_fluid_range.py` "Best Performers" (`idxmax()` picks) still operate on the unclamped sweep — grepped the file for a `power_fluid_max` comparison/clip and found none. |
| P1-17 | OPEN | `woffl/gui/tabs/batch_run.py:57` and `:289` (`df = df[~df["qoil_std"].isna()]`) | Failed combos still silently dropped with no surfaced count/rate. Contrast `woffl/gui/tabs/power_fluid_range.py:508-535`, which computes `successful_df` and shows `st.metric("Successful Runs", ...)` / `st.metric("Success Rate", ...)` — batch_run.py has no equivalent. |
| P1-18 | PARTIAL | `woffl/gui/app.py:115` (`needs_tests = "all_well_tests_df" not in st.session_state`), `:191/:196` (sets the key to `None` on failure); `woffl/gui/workflow_steps/step3_configure_optimize.py:511` reads `st.session_state.get("all_well_tests_df")` directly, feeding `_build_actual_maps` — no retry | Root cause (app.py permanent pin + step3's direct read) is still present. However `woffl/gui/scotts_tools/_common.py:119-141` (`get_vogel_for_wells`) no longer matches the original complaint: it now independently retries via `fetch_well_tests(months_back)` (its own `@st.cache_data` call) and only falls back to the stale session_state value in the `except` branch — i.e. that consumer already self-heals like the single-well path. So the bug survives in `app.py` + `step3_configure_optimize.py`, but is already resolved in `scotts_tools/_common.py`. |
| P1-19 | OPEN | `woffl/gui/tabs/batch_run.py:1211-1214` (`st.checkbox(..., value=False, key="batch_apply_calibration", ...)`) | Still a hardcoded `value=False` literal, not `st.session_state.get(key, False)` — a Solver detour will still GC this and silently revert the table to uncalibrated. |
| P1-20 | OPEN | `woffl/gui/utils.py:1241` (inside `create_well_profile`, `@st.cache_data`-decorated) | Still `print(f"Warning: {e}. Using default jetpump MD.")` — bare `print()`, invisible on Databricks Apps, and inside a cached function so it won't even re-fire on cache hits. |
| P1-27 | FIXED | `woffl/gui/scotts_tools/well_sort.py:1125-1140` (`_effective_wc`, returns `(wc, basis)`, prefers `TotalWC`, falls back to form-basis only when total is NaN) and `~1246-1345` (`add_shut_decision` refuses a "BOL trial" grant on form-basis 90-day history, emits "Verify — form-basis history" instead) | Comments cite "P1-27" at lines 1131, 1144, 1159, 1258, 1288. Both the basis-mismatch and the silent-fallback parts of the finding are addressed. |
| P1-28 | FIXED | `woffl/gui/scotts_tools/well_sort.py:1208-1237` (`add_online_decision`) | Outlier gate now checks both `oil_down` (OilDev) **and** `wat_up` (WatDev, confirmed already computed in `woffl/assembly/well_sort_client.py:630`); either trips "Verify before SI" instead of an unconditional "SI candidate". Comments cite "P1-28" at lines 1167, 1211. |
| P1-31 | OPEN | `woffl/gui/scotts_tools/well_sort.py:142` (`key="well_sort_pops_pads"` on the widget); read back via `st.session_state.get("well_sort_pops_pads", ["E","F","H","I","M","S"])` at lines 586, 908, 1618; pad pump limits via `_pad_limit_key()` (`:875`, returns `f"_pad_pump_limit_{pad}"`) passed straight as the `st.number_input` `key=` at `:944` | Still a pure widget key with a hard-coded fallback default, no separate non-widget mirror key found anywhere in the file — matches the widget-GC failure mode this repo's CLAUDE.md documents. |
| P1-32 | OPEN | `woffl/gui/scotts_tools/jp_washout.py:80-91` (`pump_changed` computed, stored as `"PumpChangedSinceTest"` in the scan row dict) | Grepped the whole file for `PumpChangedSinceTest` — only the one write site; never read again. `render_tab()`'s `FlagWashOut` logic (`disp["PpfRequired"] > disp["PadThreshold"]`) and displayed columns make no reference to it — computed and discarded exactly as described. |
| P1-33 | OPEN | `woffl/gui/scotts_tools/well_sort.py:110` (`if xv.empty: st.caption("...XV status unavailable...")`, Wells tab only) | Grepped the whole file for `xv.empty` — the only occurrence is the Wells-tab one. The Triage tab's data-load block (~1605-1636) fetches the same `_cached_xv_status()` but has no matching empty-check/caption. |
| P1-34 | OPEN | `woffl/gui/scotts_tools/well_sort.py:899-900` (caption: "...after shedding the worst-WC wells down to the pump's capacity...") vs `compute_pad_marginal_wc` (`~664-732`, `marginal_wc = valid.iloc[0]["WC_pad"]` after a descending sort — a plain max; `pump_limit` only feeds a separate `headroom` number, `:717-718`) | The function's own docstring (`:675-678`) was already corrected to admit "The pump limit doesn't influence the marginal," but the user-facing caption at the call site (899-900) was not updated to match — the misleading UI text the finding complains about is still live. |
| P1-35 | FIXED | `woffl/gui/scotts_tools/jp_calibration.py:37-64` (`_latest_bhp_whp_paired_per_well`, pulls BHP/WHP/date from the same test row via `groupby("well").tail(1)`); wired through `_build_calibration_input_table` (`:169-253`) and the calibration run loop (`:578-611`) | Comments tagged "[P1-35 fix]" at definition and both call sites. No stray references to the old independent `get_latest_bhp_per_well`/`get_latest_whp_per_well` remain anywhere in the file — this looks like a complete fix, not mid-flight, despite the heads-up that it might be. |

## Summary counts

- **FIXED:** P1-1, P1-3, P1-5, P1-6, P1-7, P1-11, P1-27, P1-28, P1-35 (9)
- **PARTIAL:** P1-18 (1)
- **OPEN:** P1-8, P1-9, P1-10, P1-13, P1-14, P1-15, P1-16, P1-17, P1-19, P1-20, P1-31, P1-32, P1-33, P1-34 (14)

## Open items needing fixes (ordered by severity, then rough effort)

**HIGH**
1. **P1-18 (partial)** — `woffl/gui/app.py` permanently pins `all_well_tests_df=None` for the session after one Databricks blip; `step3_configure_optimize.py`'s `_build_actual_maps` reads it directly with no retry. Fix: don't cache the failure as a terminal state — retry (or re-check `errors["tests"]`) the way `scotts_tools/_common.py` already does.

**MEDIUM — small/contained fixes**
2. **P1-19** — one-line: change batch_run.py's calibration checkbox to `value=st.session_state.get(key, False)`.
3. **P1-33** — one-line: add the same `if xv.empty: st.caption(...)` to the Triage tab that the Wells tab already has.
4. **P1-34** — one-line: reword the pad-marginal caption to match the function's own (already-corrected) docstring, or implement real capacity-shedding if that's the intended behavior.
5. **P1-32** — wire the already-computed `pump_changed`/`PumpChangedSinceTest` into the wash-out flag logic (suppress/annotate recommendations where the pump has already been changed out).
6. **P1-16** — clip the `np.arange(...)` output to `<= max` in both `utils.py` and anywhere it feeds "Best Performers" selection.
7. **P1-9 / P1-10 (LOW but trivial)** — add a `reynolds == 0` guard in `singlephase.ffactor_darcy`; clamp or switch `_zfactor_grad_school` to `_zfactor_dak` in `formgas.py`.
8. **P1-20 (LOW)** — replace the bare `print()` in `utils.py:1241` with a visible warning path (return a flag / `logger.warning`, since it's inside a cached function).

**MEDIUM — moderate effort**
9. **P1-8** — broaden the exception handling in `solopump.py`'s `_residual_walk_inward` (line 322) and bisection midpoint (line 729) to also walk past/handle the bare `JetPumpError` from `nozzle_velocity`, not just `ConvergenceError`/`ThroatEntryNoSolution`. Needs care to avoid masking genuine failures.
10. **P1-15** — swap well_profile.py's "Horizontal Deviation" computation from `md - vd` to the profile's own `hd_ray`.
11. **P1-17** — copy power_fluid_range.py's "Successful Runs"/"Success Rate" metrics pattern into batch_run.py.
12. **P1-14** — reconcile calibrated-vs-raw values in the Nelder-Mead comparison section of batch_run.py (apply the same calibration factor to both sides, or clearly label the NM result as uncalibrated).
13. **P1-31** — persist POPS pad config + pad pump limits in a non-widget session_state key (mirroring the pattern CLAUDE.md documents for surviving widget GC on app-mode switch).

**MEDIUM — larger effort**
14. **P1-13** — either wire `rho_pf` into the actual `FormWater`/PF hydrostatics path everywhere (utils.py's three run_* functions, plus pad_optimize.py's hardcoded 62.4 sites), or remove the dead knob from the sidebar/PDF/input summary entirely.

## Notes for the orchestrator

- Physics-library items (P1-1, P1-3, P1-5, P1-6, P1-7) are all fixed and consistently tagged with `# [LIBRARY change -> upstream PR to kwellis/woffl]` comments — per CLAUDE.md these need upstream-PR flags in commit messages (appears already anticipated by whoever fixed them).
- P1-8 was NOT part of the concurrent-agent fix wave and remains fully open on both sub-issues described in the original finding.
- P1-27, P1-28, and P1-35 (well_sort.py / jp_calibration.py) were flagged as possibly mid-flight but were found fully, coherently fixed — no partial/inconsistent state observed.
- P1-29's companion fix (pump-changed guard in jp_calibration.py, out of scope for this sweep) also appears to be already implemented in the same file, observed in passing.
- P1-34 is a borderline PARTIAL/OPEN call: the underlying compute function's docstring was already made honest, but the user-facing caption text was not updated — reported as OPEN because the user-visible bug (misleading caption) persists.
